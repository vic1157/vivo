# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


from typing_extensions import LiteralString
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class ReportActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model


    async def create(
        self,
        data: types.ReportCreateInput,
        include: Optional[types.ReportInclude] = None
    ) -> _PrismaModelT:
        """Create a new Report record.

        Parameters
        ----------
        data
            Report record data
        include
            Specifies which relations should be loaded on the returned Report model

        Returns
        -------
        prisma.models.Report
            The created Report record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Report record from just the required fields
        report = await Report.prisma().create(
            data={
                # data to create a Report record
                'title': 'bbadfchfja',
                'content': 'dhheabfhf',
                'date': datetime.datetime.utcnow(),
                'filePath': 'ggciceaie',
                'analysisResult': Json({'bbehjachib': True}),
                'summary': 'cadfabfehe',
                'userId': 'dgiiaaijj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ReportCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Report records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Report record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Report.prisma().create_many(
            data=[
                {
                    # data to create a Report record
                    'title': 'bfaiacjjfc',
                    'content': 'eigcfgbif',
                    'date': datetime.datetime.utcnow(),
                    'filePath': 'bagcfbhiig',
                    'analysisResult': Json({'cghideieh': True}),
                    'summary': 'biabhbdai',
                    'userId': 'idghgaicb',
                },
                {
                    # data to create a Report record
                    'title': 'fjfddhigg',
                    'content': 'hjaecfifb',
                    'date': datetime.datetime.utcnow(),
                    'filePath': 'cbbbjbfcii',
                    'analysisResult': Json({'bbejhfidcb': True}),
                    'summary': 'bgeecijdgg',
                    'userId': 'bdiicjafbj',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ReportWhereUniqueInput,
        include: Optional[types.ReportInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Report record.

        Parameters
        ----------
        where
            Report filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Report model

        Returns
        -------
        prisma.models.Report
            The deleted Report record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        report = await Report.prisma().delete(
            where={
                'id': 'bgehebiafc',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ReportWhereUniqueInput,
        include: Optional[types.ReportInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Report record.

        Parameters
        ----------
        where
            Report filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Report model

        Returns
        -------
        prisma.models.Report
            The found Report record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        report = await Report.prisma().find_unique(
            where={
                'id': 'bghffegacj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ReportWhereUniqueInput,
        include: Optional[types.ReportInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Report record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Report filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Report model

        Returns
        -------
        prisma.models.Report
            The found Report record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        report = await Report.prisma().find_unique_or_raise(
            where={
                'id': 'bhghchehcc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReportWhereInput] = None,
        cursor: Optional[types.ReportWhereUniqueInput] = None,
        include: Optional[types.ReportInclude] = None,
        order: Optional[Union[types.ReportOrderByInput, List[types.ReportOrderByInput]]] = None,
        distinct: Optional[List[types.ReportScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Report records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Report records returned
        skip
            Ignore the first N results
        where
            Report filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Report model
        order
            Order the returned Report records by any field
        distinct
            Filter Report records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Report]
            The list of all Report records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Report records
        reports = await Report.prisma().find_many(take=10)

        # find the first 5 Report records ordered by the id field
        reports = await Report.prisma().find_many(
            take=5,
            order={
                'id': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ReportWhereInput] = None,
        cursor: Optional[types.ReportWhereUniqueInput] = None,
        include: Optional[types.ReportInclude] = None,
        order: Optional[Union[types.ReportOrderByInput, List[types.ReportOrderByInput]]] = None,
        distinct: Optional[List[types.ReportScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Report record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Report filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Report model
        order
            Order the returned Report records by any field
        distinct
            Filter Report records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Report
            The first Report record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Report record ordered by the title field
        report = await Report.prisma().find_first(
            skip=1,
            order={
                'title': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ReportWhereInput] = None,
        cursor: Optional[types.ReportWhereUniqueInput] = None,
        include: Optional[types.ReportInclude] = None,
        order: Optional[Union[types.ReportOrderByInput, List[types.ReportOrderByInput]]] = None,
        distinct: Optional[List[types.ReportScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Report record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Report filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Report model
        order
            Order the returned Report records by any field
        distinct
            Filter Report records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Report
            The first Report record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Report record ordered by the content field
        report = await Report.prisma().find_first_or_raise(
            skip=1,
            order={
                'content': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ReportUpdateInput,
        where: types.ReportWhereUniqueInput,
        include: Optional[types.ReportInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Report record.

        Parameters
        ----------
        data
            Report record data specifying what to update
        where
            Report filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Report model

        Returns
        -------
        prisma.models.Report
            The updated Report record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        report = await Report.prisma().update(
            where={
                'id': 'dcgchcbbf',
            },
            data={
                # data to update the Report record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ReportWhereUniqueInput,
        data: types.ReportUpsertInput,
        include: Optional[types.ReportInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Report filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Report model

        Returns
        -------
        prisma.models.Report
            The created or updated Report record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        report = await Report.prisma().upsert(
            where={
                'id': 'bdedcabahc',
            },
            data={
                'create': {
                    'id': 'bdedcabahc',
                    'title': 'fjfddhigg',
                    'content': 'hjaecfifb',
                    'date': datetime.datetime.utcnow(),
                    'filePath': 'cbbbjbfcii',
                    'analysisResult': Json({'bbejhfidcb': True}),
                    'summary': 'bgeecijdgg',
                    'userId': 'bdiicjafbj',
                },
                'update': {
                    'title': 'fjfddhigg',
                    'content': 'hjaecfifb',
                    'date': datetime.datetime.utcnow(),
                    'filePath': 'cbbbjbfcii',
                    'analysisResult': Json({'bbejhfidcb': True}),
                    'summary': 'bgeecijdgg',
                    'userId': 'bdiicjafbj',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ReportUpdateManyMutationInput,
        where: types.ReportWhereInput,
    ) -> int:
        """Update multiple Report records

        Parameters
        ----------
        data
            Report data to update the selected Report records to
        where
            Filter to select the Report records to update

        Returns
        -------
        int
            The total number of Report records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Report records
        total = await Report.prisma().update_many(
            data={
                'date': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReportWhereInput] = None,
        cursor: Optional[types.ReportWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Report records present in the database

        Parameters
        ----------
        select
            Select the Report fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Report filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ReportCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Report.prisma().count()

        # results: prisma.types.ReportCountAggregateOutput
        results = await Report.prisma().count(
            select={
                '_all': True,
                'filePath': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ReportCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReportWhereInput] = None,
        cursor: Optional[types.ReportWhereUniqueInput] = None,
    ) -> types.ReportCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ReportCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReportWhereInput] = None,
        cursor: Optional[types.ReportWhereUniqueInput] = None,
    ) -> Union[int, types.ReportCountAggregateOutput]:
        """Count the number of Report records present in the database

        Parameters
        ----------
        select
            Select the Report fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Report filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ReportCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Report.prisma().count()

        # results: prisma.types.ReportCountAggregateOutput
        results = await Report.prisma().count(
            select={
                '_all': True,
                'analysisResult': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ReportCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ReportWhereInput] = None
    ) -> int:
        """Delete multiple Report records.

        Parameters
        ----------
        where
            Optional Report filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Report records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Report records
        total = await Report.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ReportScalarFieldKeysT'],
        *,
        where: Optional['types.ReportWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ReportAvgAggregateInput'] = None,
        sum: Optional['types.ReportSumAggregateInput'] = None,
        min: Optional['types.ReportMinAggregateInput'] = None,
        max: Optional['types.ReportMaxAggregateInput'] = None,
        having: Optional['types.ReportScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ReportCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ReportScalarFieldKeysT', 'types.SortOrder'], List[Mapping['types.ReportScalarFieldKeysT', 'types.SortOrder']]]] = None,
    ) -> List['types.ReportGroupByOutput']:
        """Group Report records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Report fields to group records by
        where
            Report filter to select records
        take
            Limit the maximum number of Report records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ReportGroupByOutput]
            A list of dictionaries representing the Report record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Report records by summary values
        # and count how many records are in each group
        results = await Report.prisma().group_by(
            ['summary'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models